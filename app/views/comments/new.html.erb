<%= form_for([@article, @article.comments.new]) do |f| %>
<% if logged_in?%>
<div id="prphotocomment">
<%=image_tag current_user.profile.photo.url(:thumb)%>

</div>
<div class="fieldcomment">	
<%= f.label :body,"add your comment" %><br />
<%= f.text_area :body,:rows=>1%>
</div>
</br>

</br>
<div class="actions">
<%= f.submit 'Add a comment' %>
</div>
<% end %>
<% end %>
